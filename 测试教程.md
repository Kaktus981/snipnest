# 文栈 SnipNest 零基础测试教程

这份教程假设你以前从来没有安装或使用过浏览器扩展。只要按照顺序操作即可，不需要编程知识。

## 先认识三个名词

- **Edge**：微软的浏览器，也就是你平时打开网页的软件。
- **浏览器扩展/插件**：安装在浏览器里的小工具。文栈就是一个 Edge 扩展。
- **侧边栏**：浏览器右侧可以展开的一块工具区域，文栈用它管理草稿和片段。

测试过程中只使用测试站提供的虚构文字。不要输入自己的密码、验证码、银行卡或证件信息。

## 第一部分：把文栈安装到 Edge

### 第1步：打开扩展管理页面

1. 打开 Microsoft Edge。
2. 点击最上方长条形的地址栏。
3. 输入：

   ```text
   edge://extensions
   ```

4. 按键盘上的 Enter（回车）键。

你会看到一个标题类似“扩展”或“管理扩展”的页面。

### 第2步：打开开发人员模式

1. 在扩展管理页面找到“开发人员模式”开关，通常在页面左侧或右上角。
2. 点击开关，把它打开。
3. 页面上会出现“加载解压缩的扩展”等按钮。

“开发人员模式”只用于加载我们正在测试、还没有发布到商店的版本。

### 第3步：加载文栈

1. 点击“加载解压缩的扩展”。
2. Windows 会出现一个选择文件夹的窗口。
3. 在文件夹位置中找到：

   ```text
   F:\codex\11\dist
   ```

4. 一定要选择 `dist` 文件夹本身，不要选择它里面的某个文件。
5. 点击“选择文件夹”。

成功后，扩展页面会出现一张“文栈 SnipNest”卡片，并且开关是开启状态。

如果插件源码刚刚修复或更新过，不需要重新选择文件夹。在这张卡片上点击一次圆形箭头形状的“重新加载”按钮，再刷新测试网页即可使用新版本。

### 第4步：把文栈固定到工具栏

1. 看 Edge 右上角，找到像拼图一样的“扩展”按钮。
2. 点击它。
3. 在列表中找到“文栈 SnipNest”。
4. 点击它右侧的图钉，让图钉进入已固定状态。

以后点击右上角的“文”图标，就能打开文栈的小窗口。

## 第二部分：启动本地测试网站

### 第1步：找到启动文件

1. 打开 Windows 文件资源管理器。
2. 进入文件夹：

   ```text
   F:\codex\11
   ```

3. 找到 `启动测试网站.cmd`。
4. 双击它。

会出现一个黑色窗口。它是测试网站的运行窗口，不是错误提示。测试期间不要关闭它。

### 第2步：在 Edge 打开测试网站

1. 回到 Edge。
2. 在地址栏输入：

   ```text
   http://127.0.0.1:4173/test-site/
   ```

3. 按 Enter。

如果页面顶部显示“文栈测试站”，说明启动成功。

`127.0.0.1` 代表“这台电脑自己”。页面不会上传到互联网。

## 第三部分：按照顺序测试功能

测试站上有8个编号区域。每完成一项，就勾选区域底部的完成框，页面顶部会记录进度。

### 测试1：逐站授权

1. 保持测试站页面打开。
2. 点击 Edge 右上角的文栈图标。
3. 点击“保护并打开文栈”。侧边栏会立即同步打开。
4. 如果 Edge 询问是否允许读取当前网站，点击允许。
5. 点击“允许”后，后台会自动完成启用；不需要再次点击扩展按钮。
6. 侧边栏的当前页面应该自动变为绿色“保护中”。
7. 按 `Ctrl + R` 刷新测试页面，然后勾选测试站第1项。

通过标准：文栈只获得 `http://127.0.0.1:4173` 的权限，没有要求读取其他网站。

### 测试2：自动保存与刷新恢复

1. 在测试站第2项点击“只填入1个字”。
2. 等待至少2秒，输入框旁的文栈状态应该显示“已保存”。
3. 打开侧边栏“草稿”，展开“短草稿”，确认能看到这1个字。
4. 回到网页，点击“填入示例文字”。
5. 页面会填入一段虚构的个人介绍，等待至少2秒。
6. 点击“等待2秒后刷新”。
7. 页面刷新后，个人介绍输入框会重新变空，这是正常现象。
8. 点击空白的“个人介绍”输入框。
9. 输入框右侧应该出现一个紫色的“文”。
10. 点击“文”，再点击“恢复上次内容”。

通过标准：1个字能保存并进入短草稿区域；刷新前的长示例文字也能恢复。

如果没有出现“文”，先确认插件小窗口显示“保护中”，然后再刷新页面一次。

### 测试3：字段历史版本

1. 找到测试站第3项“项目经历”。
2. 点击“A 建立第一个版本”。
3. 等待1秒。
4. 点击“B 修改为第二版”。
5. 等待1秒。
6. 点击“C 修改为第三版”。
7. 点击 Edge 右上角的文栈图标。
8. 点击“打开文栈”，浏览器右侧会出现侧边栏。
9. 在侧边栏点击“草稿”。
10. 找到“项目经历”。
11. 点击“查看历史”。

通过标准：历史区域中存在多个版本，每个版本显示保存时间和字数，最多只会保留最近5份。

继续测试意外清空保护：

1. 确认输入框中是第三版长文字，并等待2秒。
2. 点击“模拟一次性误删全部文字”。
3. “文”面板和侧边栏应出现“疑似误删”。
4. 点击“恢复删除前内容”，第三版文字应该重新出现。
5. 再模拟一次清空，点击“这是有意修改”，提醒应该消失。

通过标准：大段文字一次减少80%以上时保留删除前快照，正常逐字删除不会反复提醒。

### 测试4：动态输入框与富文本

1. 在测试站第4项点击“创建动态输入框”。
2. 点击“填入动态字段测试文字”。
3. 点击新出现的动态输入框，右侧应该出现“文”。
4. 在同一区域找到“富文本编辑区域”。
5. 点击“填入富文本测试内容”。
6. 点击富文本区域，右侧也应该出现“文”。
7. 打开文栈侧边栏的“草稿”页面。

通过标准：草稿列表中可以找到动态补充说明和项目总结。富文本首版只保存纯文字，不保存颜色、图片或复杂排版。

### 测试5：敏感字段过滤

1. 找到测试站第5项。
2. 依次点击“测试密码”“短信验证码”“银行卡号”。
3. 这些字段右侧都不应该出现“文”。
4. 在文栈侧边栏的草稿页面确认没有这些内容。
5. 点击“打开支付页面拦截测试”。
6. 在模拟支付页面点击两个输入框，仍然不应该出现“文”。
7. 点击 Edge 右上角文栈图标，应看到黄色“敏感页面”和“此页面自动禁用”。
8. 点击页面底部的“返回全功能测试站”。

通过标准：普通页面中的敏感字段和整个模拟支付页面都不会被保存。

测试页里的卡号是行业常用的虚构测试字符串，不是真实银行卡。不要替换成自己的信息。

### 测试6：把草稿升级为永久片段

1. 打开文栈侧边栏。
2. 点击“草稿”。
3. 找到“个人介绍”。
4. 点击“升级片段”。
5. 片段名称填写“个人介绍”。
6. 分类填写“求职”。
7. 标签填写：

   ```text
   个人介绍，前端
   ```

8. 点击“保存片段”。
9. 打开侧边栏的“片段”页面。

通过标准：“个人介绍”出现在片段列表中，并带有“永久”标记；原来的临时草稿会从草稿列表消失。

### 测试7：本地推荐与手动插入

1. 回到测试站第7项。
2. 点击空白的“个人介绍”输入框。
3. 点击右侧出现的“文”。
4. 浮动面板应该推荐刚才保存的“个人介绍”。
5. 点击推荐卡片。

也可以在侧边栏“当前页面”中查看推荐，然后点击“预览后插入”。

通过标准：文字进入输入框，下面的“插入事件检测”变成绿色，说明插件正确触发了网页的输入事件。

### 测试8：草稿整理、保留时间、导出和撤销权限

1. 打开侧边栏的“草稿”。
2. 依次选择“按网站”“按日期”“按字段”，确认草稿重新分组。
3. 保持任意一种选择，关闭再打开侧边栏，确认选择仍被保留。
4. 勾选一个分组或多份草稿，点击“延长30天”。
5. 被选择草稿的状态应该变成30天。
6. 打开“隐私设置”，确认默认保留时间是7天、最少字符数是1字。
7. 点击“导出 JSON”，Edge 会下载一个名称类似 `snipnest-2026-08-28.json` 的文件。
8. 在“已授权网站”找到 `http://127.0.0.1:4173`，点击“撤销”。
9. 刷新测试页面，再点击普通长文本框，右侧不应该出现“文”。

通过标准：导出文件成功生成，撤销权限后文栈立即停止在测试站工作。

自动过期本身需要等待设定时间，无法在几分钟内人工观察。项目的自动化测试已经用模拟时间验证了7天后删除；如果想长期人工验证，可以将默认时间设置为1天，第二天再次打开侧边栏检查。

## 可选：测试清除全部数据

只有在其他测试全部完成并且已经导出以后再做：

1. 进入侧边栏“隐私设置”。
2. 点击“清除全部”。
3. Edge 会询问是否确定。
4. 确认后，所有草稿和永久片段都会被删除，无法撤销。

这是破坏性操作。如果还想继续测试片段推荐，可以暂时跳过。

## 如何结束测试

1. 关闭测试网站页面。
2. 找到启动测试网站时出现的黑色窗口。
3. 点击黑色窗口右上角的 `X` 关闭它。

文栈扩展仍然保留在 Edge 中。如果想移除：

1. 打开 `edge://extensions`。
2. 找到“文栈 SnipNest”。
3. 点击“删除”。

## 常见问题

### 双击启动文件后窗口马上消失

确认项目依赖已经安装。当前工作区已安装依赖；如果文件被移动到其他电脑，需要先在项目目录执行 `npm install`。

### 打不开测试网址

确认黑色的测试网站窗口仍然开着，并且地址是：

```text
http://127.0.0.1:4173/test-site/
```

不要使用 `https`，也不要漏掉最后的 `/test-site/`。

### 输入框右侧没有“文”

依次检查：

1. 文栈扩展是否已开启。
2. 插件小窗口是否显示绿色“保护中”。
3. 授权以后是否刷新过测试页面。
4. 当前字段是否是密码、验证码、支付等敏感字段。

### 草稿没有出现

新版默认从第1个非空字符开始保存，并在停止输入约1秒后完成。1至9字草稿默认收在“短草稿”折叠区，请先将它展开；如果你在“隐私设置”提高过最少字符数，则需要达到自己设置的数量。

### 清空后没有看到恢复提醒

只有原内容至少20字，并且在一次编辑中完全清空或减少80%以上时才会触发。请先等待原文字显示“已保存”，再使用测试站的“模拟一次性误删”按钮。

### 侧边栏插入按钮不能点击

先回到网页，点击一个普通长文本框，再返回侧边栏。文栈必须知道要把片段插入哪个字段。
